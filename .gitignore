# Note: do NOT ignore "dpf" here. A bare pattern matches third_party/dpf, which kept
# that submodule's gitlink out of the index while .gitmodules still declared it - so a
# fresh clone got no travesty headers and could not build.
build/
tools/xtree/xtree
graphify-out/
release/
release-test/
